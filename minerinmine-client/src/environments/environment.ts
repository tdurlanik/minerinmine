/**
 * Üretim (production) ortamı ayarları.
 * `ng build` çalıştırıldığında bu dosya kullanılır.
 *
 * Gerçek bir yayında burası sunucunuzun adresi olur, örn: 'https://api.minerinmine.com/api'
 */
export const environment = {
  production: true,
  apiUrl: 'http://localhost:5080/api'
};
